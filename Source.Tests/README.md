# Source.Tests

In-game test cases for this mod, run by the RimAgentic toolkit bridge under `-synapse-test`.
Builds into `../TestAssemblies/` (dev-only; never shipped, never auto-loaded by RimWorld).

Writing rules, phases, and the sentinel-case contract:
`rimworld-claude-dev-tools/modding-knowledge/07-in-game-tests.md`.

Do not move this project under `Source/` and never output to `Assemblies/`.
